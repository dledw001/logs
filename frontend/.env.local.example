# Backend origin for rewrites (keeps cookies same-site with the Next app)
API_BASE_URL=http://localhost:4000

# Leave empty to rely on rewrites (recommended for cookie-based auth).
# If you point directly at another origin, set this and also configure backend cookies to SameSite=None; Secure.
NEXT_PUBLIC_API_BASE_URL=
